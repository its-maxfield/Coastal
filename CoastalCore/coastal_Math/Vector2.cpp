#include "Vector2.h"
#include <string>
#include <iostream>
#include <math.h>

using namespace std;

Vector2:Vector2(float x, float y)
{
    this->setX(x);
    this->setY(y);
}

Vector2:Vector2(float x)
{
    this->setX(x);
    this->setY(x);
}

Vector2:Vector2()
{
    this->setX(0.0);
    this->setY(0.0);
}

Vector2:printVec()
{
    -
}
